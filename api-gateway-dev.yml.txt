spring:
  application:
    name: api-gateway
  profiles:
    active:
    - dev
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: : http://localhost/auth-service
  data:
    redis:
      host: localhost:6371
  cloud:
    gateway:
      server:
       webflux:
        default-filters:
        - name: RequestRateLimiter
          args:
            redis-rate-limiter.replenishRate : 5
            redis-rate-limiter-burstCapacity : 10
        routes:
          - id : account-service
            uri : lb://ACCOUNT-SERVICE
            predicates :
                - path = /api/accounts/**
            filters:
                - StripPrefix=1
eureka:
  client:
    service-url:
      defaultzone: http://localhost:8761/eureka

server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include:
        - health
        - info
        - prometheus

